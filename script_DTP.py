# CALLS scripts.py to
# (1) generate data
# (2) train models
# (3) plot results

import gen_data

# FOR LOOP over parameters
# update parameters in YAML

import train_models
import plot_models